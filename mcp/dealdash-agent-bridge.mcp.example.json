{
  "mcpServers": {
    "dealdash-agent-bridge": {
      "command": "pnpm",
      "args": [
        "exec",
        "tsx",
        "./plugins/dealdash-agent-bridge/scripts/server.ts"
      ],
      "env": {
        "DEALDASH_AGENT_BASE_URL": "https://dealdash.neonoir.ai",
        "DEALDASH_AGENT_CHANNEL": "mcp:generic",
        "DEALDASH_AGENT_ACTOR_ID": "dealdash-agent-bridge"
      },
      "note": "Normal setup uses agent_auth_start to send the user a DealDash login approval link. Do not ask normal users for service secrets or internal IDs."
    }
  }
}
